<div>
    {{#each jobs as |job|}}
    <div class="cards-containerj">
        <div class="cardj">
            <a href="/job/{{job.id}}">
                <h2>{{job.title}}</h2>
                <p>{{job.location}}</p>
                <p>Position: {{job.qualification}}</p>
            </a>
        </div>
    </div>
    {{/each}}
</div>